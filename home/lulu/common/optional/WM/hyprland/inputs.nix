{ ... }:

{
  wayland.windowManager.hyprland.settings = {
    input = {
      kb_layout = "us";
      kb_variant = "intl";
      kb_options = "caps:swapescape";

      sensitivity = -0.4;
      accel_profile = "flat";

      repeat_delay = 300;
      repeat_rate = 50;

    };
  };
}
